# CLAUDE.md — Автор24 AI Bot (Avtor24 Automation System)

## Обзор проекта

Полностью автоматизированная система, которая:
1. Мониторит новые заказы на avtor24.ru
2. Анализирует заказы и выбирает подходящие
3. Автоматически ставит ставки
4. Генерирует работы с помощью AI (GPT-4o)
5. Проверяет антиплагиат
6. Отправляет готовые работы заказчику
7. Ведёт диалог с заказчиком
8. Предоставляет дашборд владельцу

**Стек:** Python 3.12, FastAPI, Playwright, OpenAI API, Docker, Railway

---

## СЕКЦИЯ 1: Структура проекта

```
avtor24-bot/
├── CLAUDE.md
├── .env.example
├── .gitignore
├── Dockerfile
├── docker-compose.yml
├── railway.toml
├── requirements.txt
├── package.json                  # для docx генерации через docx-js
│
├── src/
│   ├── __init__.py
│   ├── main.py                   # FastAPI app + scheduler
│   ├── config.py                 # Конфиг из .env
│   │
│   ├── scraper/                  # Парсинг Автор24
│   │   ├── __init__.py
│   │   ├── auth.py               # Авторизация, управление сессией
│   │   ├── orders.py             # Парсинг ленты заказов
│   │   ├── order_detail.py       # Парсинг деталей заказа
│   │   ├── bidder.py             # Постановка ставок
│   │   ├── chat.py               # Чтение/отправка сообщений в чат
│   │   ├── file_handler.py       # Скачивание файлов заказчика / загрузка готовых
│   │   └── browser.py            # Playwright browser manager
│   │
│   ├── analyzer/                 # Анализ заказов
│   │   ├── __init__.py
│   │   ├── order_scorer.py       # Оценка: брать или нет
│   │   ├── price_calculator.py   # Расчёт оптимальной ставки
│   │   └── file_analyzer.py      # Анализ прикреплённых файлов (PDF/DOCX методичек)
│   │
│   ├── generator/                # Генерация работ
│   │   ├── __init__.py
│   │   ├── router.py             # Роутер по типу работы
│   │   ├── essay.py              # Эссе, сочинения
│   │   ├── referat.py            # Рефераты, доклады
│   │   ├── coursework.py         # Курсовые работы
│   │   ├── diploma.py            # Дипломные/ВКР (пошаговая генерация по главам)
│   │   ├── homework.py           # Контрольные, ответы на вопросы, решение задач
│   │   ├── presentation.py       # Презентации
│   │   ├── translation.py        # Переводы
│   │   ├── code_task.py          # Задачи по программированию
│   │   ├── copywriting.py        # Копирайтинг, рерайт
│   │   ├── business_plan.py      # Бизнес-планы
│   │   ├── practice_report.py    # Отчёты по практике
│   │   ├── review.py             # Рецензии, вычитка
│   │   ├── uniqueness.py         # Повышение уникальности
│   │   └── prompts/              # Системные промпты для каждого типа
│   │       ├── essay_system.txt
│   │       ├── coursework_system.txt
│   │       ├── diploma_system.txt
│   │       └── ...
│   │
│   ├── docgen/                   # Генерация DOCX файлов
│   │   ├── __init__.py
│   │   ├── builder.py            # Сборка DOCX через docx-js (Node.js subprocess)
│   │   ├── templates/            # Шаблоны оформления
│   │   │   ├── coursework.js     # Шаблон курсовой (титульник, содержание, шрифт 14, интервал 1.5)
│   │   │   ├── diploma.js        # Шаблон ВКР
│   │   │   ├── referat.js        # Шаблон реферата
│   │   │   └── essay.js          # Шаблон эссе
│   │   └── formatter.py          # Форматирование текста по ГОСТ
│   │
│   ├── sandbox/                  # Песочница для кода
│   │   ├── __init__.py
│   │   ├── executor.py           # Запуск кода в Docker-контейнере
│   │   ├── languages.py          # Конфиги языков (Python, JS, Java, C++, C#)
│   │   └── Dockerfiles/
│   │       ├── python.Dockerfile
│   │       ├── node.Dockerfile
│   │       ├── java.Dockerfile
│   │       └── cpp.Dockerfile
│   │
│   ├── antiplagiat/              # Проверка уникальности
│   │   ├── __init__.py
│   │   ├── checker.py            # Основной чекер
│   │   ├── etxt.py               # Проверка через ETXT API
│   │   ├── textru.py             # Проверка через text.ru API
│   │   └── rewriter.py           # Авторерайт если уникальность < порога
│   │
│   ├── chat_ai/                  # AI-ведение диалога с заказчиком
│   │   ├── __init__.py
│   │   ├── responder.py          # Генерация ответов заказчику
│   │   └── prompts/
│   │       └── chat_system.txt   # Промпт: ты автор, веди себя естественно
│   │
│   ├── notifications/            # Реалтайм уведомления через WebSocket
│   │   ├── __init__.py
│   │   ├── websocket.py          # WebSocket manager для реалтайм пуш
│   │   ├── events.py             # Генерация событий/уведомлений
│   │   └── browser_push.py       # Web Push API (опционально)
│   │
│   ├── dashboard/                # Веб-дашборд
│   │   ├── __init__.py
│   │   ├── app.py                # FastAPI роутер для дашборда
│   │   ├── auth.py               # Авторизация по логину/паролю
│   │   ├── static/               # Frontend (HTML/CSS/JS)
│   │   │   ├── index.html
│   │   │   ├── login.html
│   │   │   ├── styles.css
│   │   │   └── app.js
│   │   └── templates/
│   │       └── ...
│   │
│   └── database/                 # Хранение данных
│       ├── __init__.py
│       ├── models.py             # SQLAlchemy модели
│       ├── crud.py               # CRUD операции
│       └── connection.py         # Подключение к PostgreSQL
│
├── scripts/
│   ├── generate_docx.js          # Node.js скрипт для генерации DOCX
│   └── setup.sh                  # Установка зависимостей
│
└── tests/
    ├── test_scraper.py
    ├── test_generator.py
    ├── test_antiplagiat.py
    └── test_sandbox.py
```

---

## СЕКЦИЯ 2: Переменные окружения (.env)

```env
# Автор24
AVTOR24_EMAIL=<email для входа>
AVTOR24_PASSWORD=<пароль от аккаунта>
AVTOR24_BASE_URL=https://avtor24.ru

# OpenAI
OPENAI_API_KEY=<ключ>
OPENAI_MODEL_MAIN=gpt-4o           # для генерации работ
OPENAI_MODEL_FAST=gpt-4o-mini      # для анализа, чата, скоринга

# Антиплагиат
ETXT_API_KEY=<ключ ETXT если есть>
TEXTRU_API_KEY=<ключ text.ru>
MIN_UNIQUENESS=50                   # минимальный порог уникальности (зависит от заказа)

# Дашборд (единственный центр управления, без Telegram)
DASHBOARD_USERNAME=<логин>
DASHBOARD_PASSWORD_HASH=<bcrypt хеш пароля>
DASHBOARD_SECRET_KEY=<случайная строка для JWT>

# База данных
DATABASE_URL=postgresql://user:pass@host:5432/avtor24bot

# Прокси
PROXY_RU=socks5://user:pass@ip:port    # Российский резидентный прокси для avtor24.ru
# Нужен потому что Railway хостится в Нидерландах, а avtor24.ru — российский сайт
# OpenAI API — прокси НЕ нужен, Railway напрямую достучится

# Настройки бота
MAX_CONCURRENT_ORDERS=5             # Макс заказов одновременно
AUTO_BID=true                       # Автоматическая постановка ставок
BID_COMMENT_TEMPLATE=default        # Шаблон комментария к ставке
MIN_PRICE_RUB=300                   # Минимальная цена заказа
MAX_PRICE_RUB=50000                 # Максимальная цена
SCAN_INTERVAL_SECONDS=60            # Интервал сканирования новых заказов
SPEED_LIMIT_MIN_DELAY=30            # Мин задержка между действиями (антибан)
SPEED_LIMIT_MAX_DELAY=120           # Макс задержка
```

---

## СЕКЦИЯ 3: Скрапер (src/scraper/)

### 3.1 Browser Manager (browser.py)

```
Используем Playwright (async) с chromium.
- Запуск с российским прокси (PROXY_RU)
- User-Agent ротация (5-10 реальных Chrome UA)
- Viewport: случайный из популярных (1920x1080, 1366x768, 1536x864)
- Случайные задержки между действиями (SPEED_LIMIT)
- Сохранение и переиспользование cookies/session в файл
- Headless=True для Railway
```

### 3.2 Авторизация (auth.py)

```
1. Проверить наличие сохранённой сессии (cookies.json)
2. Если есть — загрузить cookies, проверить валидность (зайти на главную, проверить что залогинен)
3. Если нет или невалидна:
   a. Перейти на страницу логина
   b. Заполнить email/пароль
   c. Нажать "Войти"
   d. Сохранить cookies
4. Периодически обновлять сессию (каждые 30 мин заходить на главную)
```

### 3.3 Парсинг ленты заказов (orders.py)

```
URL: https://avtor24.ru/order/search

Парсить из каждой карточки заказа:
- order_id (из URL или атрибута)
- title (заголовок заказа)
- work_type (Курсовая работа, Реферат, Эссе, etc.)
- subject (предмет: Экономика, Менеджмент, etc.)
- deadline (дата сдачи)
- time_left (осталось X дней/часов)
- budget (цена в рублях или "Договорная цена")
- bid_count (количество ставок)
- files_count (количество прикреплённых файлов)
- customer_online (онлайн ли заказчик)
- customer_badge (Постоянный клиент, Быстрый заказ, etc.)
- description_preview (превью описания)

Фильтры при парсинге (правая панель на сайте):
- Типы работ: все выбранные при регистрации
- Предметы: все выбранные
- Бюджет: от MIN_PRICE_RUB до MAX_PRICE_RUB
- Заказы без ставок / Меньше 3 ставок — приоритет

Пагинация: парсить первые 3-5 страниц.
Частота: каждые SCAN_INTERVAL_SECONDS секунд.
Дедупликация: хранить order_id уже обработанных в БД.
```

### 3.4 Парсинг деталей заказа (order_detail.py)

```
При клике на заказ из ленты, парсить:
- Полное описание задания
- Тип работы, предмет, кол-во страниц, шрифт, интервал
- Требуемая оригинальность (%) и система антиплагиата (ETXT, Антиплагиат.ру, etc.)
- Прикреплённые файлы (скачать все)
- Гарантийный срок
- Бюджет заказчика / Средняя ставка
- Информация о заказчике
```

### 3.5 Постановка ставок (bidder.py)

```
1. Рассчитать оптимальную цену (price_calculator.py)
2. Заполнить поле "Ваша ставка" (цена в рублях)
3. Заполнить "Ваш комментарий" из шаблона или сгенерировать через GPT-4o-mini:
   - Короткое, дружелюбное сообщение
   - Упомянуть опыт в теме заказа
   - Обещать сдачу вовремя
   - Не более 2-3 предложений
4. Нажать "Поставить ставку"
5. Записать в БД: order_id, bid_price, bid_time, status='bid_placed'
```

### 3.6 Чат с заказчиком (chat.py)

```
Мониторинг:
- Каждые 2-3 минуты проверять вкладку "Мои заказы" → чаты с новыми сообщениями
- Парсить новые сообщения от заказчика

Ответ:
- Передать сообщение в chat_ai/responder.py
- AI генерирует ответ от имени автора
- Отправить ответ в чат

Контекст для AI:
- Описание заказа
- Вся история переписки
- Текущий статус (в работе, ожидает оплаты, etc.)
- Системный промпт: "Ты автор на Автор24, общаешься с заказчиком..."
```

### 3.7 Загрузка готовой работы (file_handler.py)

```
1. Сгенерированный DOCX файл
2. Перейти в чат с заказчиком
3. Прикрепить файл
4. Отправить сопроводительное сообщение (GPT-4o-mini):
   "Добрый день! Работа готова, загружаю. Если будут правки — пишите, исправлю."
5. Обновить статус в БД: status='delivered'
```

---

## СЕКЦИЯ 4: Анализатор заказов (src/analyzer/)

### 4.1 Скоринг заказов (order_scorer.py)

```
GPT-4o-mini получает данные заказа и возвращает JSON:

{
  "score": 0-100,           // общая привлекательность
  "can_do": true/false,     // может ли AI выполнить
  "estimated_time_min": 30, // время генерации в минутах
  "estimated_cost_rub": 5,  // себестоимость (токены AI)
  "reason": "..."           // почему да/нет
}

Критерии скоринга:
- Тип работы: эссе/реферат = +30, курсовая = +20, диплом = +10, код = +15
- Предмет: гуманитарные = +20, экономические = +15, технические = +5
- Цена: выше средней = +20, ниже = -10
- Дедлайн: >3 дней = +10, <24ч = -20
- Конкуренция: 0 ставок = +20, >5 ставок = -15
- Уникальность: <60% = +10, >80% = -5 (сложнее достичь)
- Наличие методички/файлов: +10 (точнее ТЗ = лучше результат)

Порог для авто-ставки: score >= 60
```

### 4.2 Расчёт цены (price_calculator.py)

```
Логика:
1. Если указан бюджет заказчика — ставить 85-95% от бюджета
2. Если "Договорная цена":
   - Посмотреть среднюю ставку (она отображается справа)
   - Ставить 90-100% от средней
3. Если нет ставок и нет бюджета:
   - Рассчитать по формуле: base_price[work_type] × pages × complexity_factor
   
   base_price:
   - Эссе: 150 руб/стр
   - Реферат: 120 руб/стр
   - Курсовая: 200 руб/стр
   - Дипломная: 250 руб/стр
   - Решение задач: 100-500 руб/задача
   - Код: 500-3000 руб/задача

4. Минимальная ставка: MAX(300, расчётная цена)
5. Учесть комиссию: заказчик платит 2.63x от ставки, мы получаем 97% от ставки
```

### 4.3 Анализ файлов (file_analyzer.py)

```
Если к заказу прикреплены файлы (методички, инструкции):
1. Скачать все файлы
2. Определить тип: PDF → извлечь текст через PyMuPDF, DOCX → через python-docx
3. Передать текст методички в GPT-4o-mini для суммаризации:
   - Требования к оформлению
   - Структура работы
   - Темы/разделы
   - Объём
4. Использовать суммари как дополнительный контекст при генерации
```

---

## СЕКЦИЯ 5: Генератор работ (src/generator/)

### 5.1 Роутер (router.py)

```python
# Маппинг типа работы → генератор
GENERATORS = {
    "Эссе": essay.generate,
    "Сочинение": essay.generate,
    "Реферат": referat.generate,
    "Доклад": referat.generate,
    "Курсовая работа": coursework.generate,
    "Выпускная квалификационная работа (ВКР)": diploma.generate,
    "Дипломная работа": diploma.generate,
    "Контрольная работа": homework.generate,
    "Решение задач": homework.generate,
    "Ответы на вопросы": homework.generate,
    "Презентации": presentation.generate,
    "Перевод": translation.generate,
    "Задача по программированию": code_task.generate,
    "Копирайтинг": copywriting.generate,
    "Повышение уникальности текста": uniqueness.generate,
    "Бизнес-план": business_plan.generate,
    "Отчёт по практике": practice_report.generate,
    "Рецензия": review.generate,
    "Вычитка и рецензирование работ": review.generate,
    "Статья": referat.generate,     # похожая структура
    "Научно-исследовательская работа (НИР)": coursework.generate,
    "Индивидуальный проект": coursework.generate,
    "Лабораторная работа": homework.generate,
    "Набор текста": copywriting.generate,
    "Автореферат": referat.generate,
    "Аннотация": essay.generate,
    "Онлайн-консультация": None,    # пропускаем, требует реалтайм
    "Помощь on-line": None,
    "Маркетинговое исследование": coursework.generate,
    "Творческая работа": essay.generate,
    "Статья ВАК/Scopus": referat.generate,
    "Монография": diploma.generate,
    "Подбор темы работы": None,      # GPT-4o-mini в чате предлагает темы
    "Разбор отчёта Антиплагиат": None,
    "Гуманизация работы": uniqueness.generate,
    "Проверка работы": review.generate,
    "Другое": homework.generate,
}
```

### 5.2 Общий паттерн генерации (все типы)

```
1. Сформировать system prompt (из файла prompts/xxx_system.txt)
2. Сформировать user prompt:
   - Тема работы
   - Описание/ТЗ заказчика
   - Содержание методички (если есть)
   - Требования: кол-во страниц, шрифт, интервал, уникальность
   - Предмет
3. Вызвать GPT-4o
4. Получить текст
5. Сформировать DOCX через docgen/builder.py
6. Проверить антиплагиат
7. Если уникальность < порога → рерайт через rewriter.py → повторить проверку (до 3 раз)
8. Вернуть путь к готовому DOCX файлу
```

### 5.3 Курсовая работа (coursework.py) — типичный пример

```
Шаг 1: Генерация оглавления
   Промпт → GPT-4o → JSON с планом:
   {
     "title": "...",
     "introduction": true,
     "chapters": [
       {"number": 1, "title": "...", "subsections": ["1.1 ...", "1.2 ..."]},
       {"number": 2, "title": "...", "subsections": ["2.1 ...", "2.2 ..."]},
       {"number": 3, "title": "...", "subsections": ["3.1 ..."]}
     ],
     "conclusion": true,
     "bibliography": true
   }

Шаг 2: Генерация введения (1-2 страницы)
   Контекст: тема + план + требования
   
Шаг 3: Генерация каждой главы (по 5-10 страниц)
   Для каждой главы:
   - Контекст: тема + план + предыдущие главы (суммари) + текущая глава
   - Для каждого подраздела — отдельный вызов если глава большая
   
Шаг 4: Генерация заключения
   Контекст: тема + план + ключевые выводы из каждой главы

Шаг 5: Генерация библиографии
   Промпт: "Составь список из 15-25 источников по теме, включая:
   - 5-7 учебников (реальные авторы, реальные издательства)
   - 3-5 статей из журналов
   - 3-5 нормативных документов (если тема правовая/экономическая)
   - 2-3 интернет-источника
   Формат: ГОСТ Р 7.0.5-2008"
   
   ВАЖНО: Библиография — слабое место AI, часто галлюцинирует.
   Решение: использовать web search для верификации источников
   или иметь базу реальных источников по популярным предметам.
   
Шаг 6: Сборка в DOCX (см. секцию 6)
```

### 5.4 Дипломная/ВКР (diploma.py)

```
Аналогично coursework.py, но:
- 80-100 страниц → 20-30 API вызовов
- 3-4 главы по 15-25 страниц
- Обязательно: титульный лист, задание на ВКР, аннотация, содержание
- Приложения (если требуется)
- Библиография: 30-50 источников
- Суммарно ~45,000 output токенов
- Оценка стоимости API: $0.60-1.00 (55-90₽)
- Время генерации: 15-30 минут
```

### 5.5 Задачи по программированию (code_task.py)

```
1. GPT-4o получает условие задачи
2. Генерирует код
3. Код отправляется в sandbox/executor.py
4. Docker-контейнер запускает код
5. Результат:
   a. Успех (exit code 0) → возвращаем код + вывод
   b. Ошибка → traceback передаётся обратно в GPT-4o
   c. GPT-4o правит код → повторный запуск
   d. Цикл до 5 итераций
6. Если есть тесты от заказчика — прогоняем их тоже
7. Результат: файл с кодом + скриншот/лог вывода

Языки:
- Python 3.12
- JavaScript (Node.js 20)
- Java 17
- C++ (g++ 13)
- C# (.NET 8)

Ограничения песочницы:
- Таймаут: 30 секунд
- Память: 256MB
- Без сети
- Read-only filesystem (кроме /tmp)
```

---

## СЕКЦИЯ 6: Генерация DOCX (src/docgen/)

### Используем docx-js (Node.js) через subprocess

```
Почему docx-js а не python-docx:
- Более надёжная генерация
- Лучшая поддержка стилей и TOC
- Проверенная интеграция (см. /mnt/skills/public/docx/SKILL.md)

Процесс:
1. Python генерирует JSON с содержимым работы
2. JSON передаётся в Node.js скрипт (scripts/generate_docx.js)
3. Скрипт формирует DOCX файл
4. Возвращает путь к файлу
```

### Требования к оформлению (ГОСТ):

```
Шрифт: Times New Roman (или из заказа: 12/14 pt)
Интервал: 1.5 (или из заказа)
Поля: левое 3см, правое 1.5см, верхнее 2см, нижнее 2см
Абзацный отступ: 1.25 см
Выравнивание: по ширине
Нумерация страниц: внизу по центру, начиная со 2-й страницы
Заголовки: жирные, по центру (глава) / с абзацного отступа (подраздел)
Содержание: автогенерируемое
Титульный лист: без нумерации

Данные для титульного листа парсить из заказа:
- Вуз (если указан)
- Предмет
- Тема
- Тип работы
- Имя заказчика (если доступно, иначе пропуск)
```

---

## СЕКЦИЯ 7: Антиплагиат (src/antiplagiat/)

### 7.1 Проверка уникальности (checker.py)

```
Стратегия:
1. Определить какую систему требует заказчик (ETXT, Антиплагиат.ру, text.ru)
2. Если ETXT — проверить через ETXT API
3. Если text.ru — проверить через text.ru API
4. Если Антиплагиат.ру — сначала проверить через text.ru (приближённо),
   т.к. у Антиплагиат.ру нет публичного API
5. Вернуть процент уникальности

Если уникальность < требуемой:
→ rewriter.py перефразирует абзацы с низкой уникальностью
→ Повторная проверка
→ До 3 итераций
```

### 7.2 Рерайтер (rewriter.py)

```
Промпт для GPT-4o:
"Перефразируй следующий текст, сохраняя смысл и академический стиль.
Не используй синонимайзер — пиши как человек, меняя структуру предложений."

Стратегия: рерайтить не весь текст, а только абзацы, помеченные
антиплагиатом как заимствованные (если API возвращает детализацию).
```

---

## СЕКЦИЯ 8: Песочница для кода (src/sandbox/)

### executor.py

```python
# Псевдокод
async def execute_code(code: str, language: str, stdin: str = "", timeout: int = 30) -> dict:
    """
    Запускает код в изолированном Docker-контейнере.
    Возвращает: {"success": bool, "stdout": str, "stderr": str, "exit_code": int}
    """
    # 1. Записать код во временный файл
    # 2. Docker run с лимитами:
    #    --memory=256m --cpus=1 --network=none --read-only
    #    --tmpfs /tmp:rw,size=64m
    #    -v /tmp/code_file:/code/main.ext:ro
    # 3. Команда запуска зависит от языка:
    #    Python: python3 /code/main.py
    #    Node: node /code/main.js
    #    Java: javac /code/Main.java && java -cp /code Main
    #    C++: g++ -o /tmp/a.out /code/main.cpp && /tmp/a.out
    # 4. Таймаут через asyncio.wait_for
    # 5. Вернуть stdout/stderr
```

### AI-цикл исправления (code_task.py)

```
for attempt in range(5):
    code = await gpt4o.generate_code(task, previous_error)
    result = await sandbox.execute_code(code, language)
    
    if result["success"]:
        return code, result["stdout"]
    
    previous_error = result["stderr"]

# Если 5 попыток не помогли — отправить владельцу в Telegram
# для ручного вмешательства
```

---

## СЕКЦИЯ 9: AI-чат с заказчиком (src/chat_ai/)

### responder.py

```
Системный промпт:
"Ты — автор на платформе Автор24. Тебя зовут [имя из профиля].
Ты студент 3 курса, учишься на прикладной информатике.
Общаешься дружелюбно, грамотно, коротко.
Отвечаешь на вопросы заказчика о заказе.
Если заказчик просит правки — соглашайся и уточняй детали.
Если спрашивает о сроках — отвечай уверенно.
НИКОГДА не упоминай AI, нейросети, ChatGPT и т.д.
НИКОГДА не пиши слишком длинные сообщения.
Максимум 2-3 предложения на ответ.

Типичные ситуации:
- 'Сможете сделать?' → 'Да, тема знакома, смогу сделать в срок.'
- 'Когда будет готово?' → 'Планирую закончить к [deadline - 1 день].'
- 'Нужны правки' → 'Конечно, скиньте что исправить, сделаю.'
- 'Какая уникальность?' → 'Делаю от [требуемый %] по [система].'
"

Контекст каждого запроса:
- Описание заказа
- Требования
- Вся история чата
- Текущий статус работы
```

---

## СЕКЦИЯ 10: Уведомления — Реалтайм на дашборде (src/notifications/)

### Вместо Telegram — всё через WebSocket на дашборд

```
Все уведомления приходят на сайт-дашборд в реалтайме через WebSocket.
Telegram-бот НЕ используется. Дашборд — единственный центр управления.

Технология: FastAPI WebSocket + EventSource (SSE как fallback)
Фронтенд: WebSocket клиент + звуковое оповещение + push-уведомления браузера

Типы уведомлений (отображаются в колокольчике на дашборде):

1. 🆕 НОВЫЙ ЗАКАЗ НАЙДЕН:
   {
     "type": "new_order",
     "order_id": "12345",
     "title": "Экономика предприятия",
     "work_type": "Курсовая работа",
     "budget": 3000,
     "income": 2910,
     "deadline": "2026-02-15",
     "score": 85,
     "bid_placed": true,
     "bid_price": 2800
   }

2. 🎉 ЗАКАЗ ПРИНЯТ:
   {
     "type": "order_accepted",
     "order_id": "12345",
     "title": "...",
     "status": "generating"
   }

3. 📤 РАБОТА ОТПРАВЛЕНА:
   {
     "type": "order_delivered",
     "order_id": "12345",
     "pages": 32,
     "uniqueness": 72.5,
     "antiplagiat_system": "ETXT",
     "income": 2910,
     "api_cost": 0.15
   }

4. 💬 НОВОЕ СООБЩЕНИЕ ОТ ЗАКАЗЧИКА:
   {
     "type": "new_message",
     "order_id": "12345",
     "customer_message": "Нужно добавить источников",
     "auto_reply": "Конечно, добавлю. Сколько примерно?",
     "auto_replied": true
   }

5. ⚠️ ОШИБКА:
   {
     "type": "error",
     "order_id": "12345",
     "error": "Антиплагиат: уникальность 38% после 3 попыток рерайта",
     "requires_attention": true
   }

6. 📊 ЕЖЕДНЕВНАЯ СВОДКА (генерируется в 22:00):
   {
     "type": "daily_summary",
     "bids_placed": 15,
     "orders_accepted": 3,
     "orders_delivered": 2,
     "income_today": 5820,
     "api_cost_today": 0.85,
     "balance": 12400
   }

Хранение: все уведомления сохраняются в таблицу notifications в БД.
На дашборде — бейдж с количеством непрочитанных.
```

---

## СЕКЦИЯ 11: Дашборд — Главный центр управления (src/dashboard/)

### Это НЕ вспомогательная панель, а ОСНОВНОЙ интерфейс владельца.
### Все уведомления, аналитика, управление — только здесь.

### Технологии:

```
Backend: FastAPI (отдельный роутер /dashboard/*)
Frontend: Single Page Application
  - HTML5 + Tailwind CSS
  - Alpine.js для реактивности
  - Chart.js для графиков
  - WebSocket для реалтайм обновлений
Авторизация: JWT + httpOnly cookies
URL: https://<railway-app>.up.railway.app/dashboard/
```

### Страницы и функциональность:

#### 1. ЛОГИН (/dashboard/login)
```
- Поля: логин + пароль
- Данные из ENV: DASHBOARD_USERNAME / DASHBOARD_PASSWORD_HASH
- JWT токен на 7 дней, httpOnly cookie
- "Запомнить меня" чекбокс
```

#### 2. ГЛАВНАЯ / OVERVIEW (/dashboard/)
```
Верхняя панель (всегда видна на всех страницах):
  - 🔔 Колокольчик уведомлений (бейдж с числом непрочитанных)
  - 🟢/🔴 Статус бота (работает / остановлен / ошибка)
  - ⏱️ Время работы бота (uptime)
  - Кнопка "Стоп/Старт" бот

Виджеты-карточки (ряд сверху):
  ┌──────────────┬──────────────┬──────────────┬──────────────┐
  │ 💰 Баланс    │ 📈 Сегодня   │ 📊 Неделя    │ 🏆 Всё время │
  │ на Автор24   │ доход        │ доход        │ доход        │
  │ 12 400₽      │ 5 820₽       │ 28 350₽      │ 142 800₽     │
  └──────────────┴──────────────┴──────────────┴──────────────┘
  ┌──────────────┬──────────────┬──────────────┬──────────────┐
  │ 📋 Активных  │ ⏳ Ставок в   │ 💸 API расход│ 📊 Чистая    │
  │ заказов      │ ожидании     │ сегодня      │ прибыль      │
  │ 3            │ 12           │ $1.25 (112₽) │ 5 708₽       │
  └──────────────┴──────────────┴──────────────┴──────────────┘

Графики:
  - Доход по дням (30 дней) — bar chart
  - Количество заказов по дням — line chart
  - Соотношение дохода и расходов на API — area chart

Лента последних событий (реалтайм через WebSocket):
  21:45 — 📤 Отправлена курсовая "Экономика предприятия" → 2910₽
  21:30 — 🆕 Новый заказ: Реферат по истории, 1200₽ → ставка поставлена
  21:15 — 💬 Заказчик #12340: "Когда будет готово?" → авто-ответ отправлен
  21:00 — 🎉 Принят заказ #12345: ВКР по менеджменту, 10000₽
  ...
```

#### 3. ЗАКАЗЫ (/dashboard/orders)
```
Вкладки:
  [Активные] [Ставки] [Выполненные] [Отклонённые] [Все]

Таблица:
  | # | Тема | Тип | Предмет | Статус | Цена | Доход | Дедлайн | Уник.% | Действия |
  |---|------|-----|---------|--------|------|-------|---------|--------|----------|
  
Статусы с цветными бейджами:
  🟡 Ставка → 🔵 Принят → 🟣 Генерация → 🟠 Проверка → 🟢 Отправлен → ✅ Завершён → 🔴 Ошибка

Клик на заказ → детальная карточка:
  - Полное описание от заказчика
  - Прикреплённые файлы (скачать)
  - AI score + причина
  - Сгенерированная работа (скачать DOCX)
  - Результат антиплагиата (% + система)
  - История чата с заказчиком
  - Лог всех действий бота по этому заказу
  - Потраченные токены + стоимость API
  - Кнопки: "Перегенерировать", "Остановить", "Ручной режим"

Фильтры:
  - По дате (от-до)
  - По типу работы
  - По предмету
  - По статусу
  - По цене (от-до)
  
Сортировка:
  - По дате, цене, дедлайну, уникальности

Экспорт: CSV, JSON
```

#### 4. АНАЛИТИКА (/dashboard/analytics)
```
Период: [Сегодня] [7 дней] [30 дней] [90 дней] [Всё время] [Кастом]

Секция "Доход":
  - График дохода по дням (bar chart)
  - Средний чек
  - Медианный чек
  - Лучший день (дата + сумма)
  - Прогноз дохода на текущий месяц

Секция "Заказы":
  - Всего ставок → принято → выполнено (конверсионная воронка)
  - Процент принятия ставок
  - Среднее время выполнения
  - Рейтинг по типам работ (pie chart):
    Курсовые 35%, Рефераты 25%, Эссе 20%, Контрольные 10%, Прочее 10%
  - Топ предметов по количеству и доходу

Секция "AI / Токены":
  ┌─────────────────────────────────────────────────────────┐
  │ OPENAI API USAGE                                        │
  ├──────────────┬──────────────┬──────────────┬────────────┤
  │ GPT-4o       │ GPT-4o-mini  │ Всего        │ Стоимость  │
  │ tokens       │ tokens       │ tokens       │ USD / RUB  │
  ├──────────────┼──────────────┼──────────────┼────────────┤
  │ Сегодня      │              │              │            │
  │ 125,400      │ 45,200       │ 170,600      │ $1.35/121₽ │
  ├──────────────┼──────────────┼──────────────┼────────────┤
  │ Месяц        │              │              │            │
  │ 3,250,000    │ 1,100,000    │ 4,350,000    │ $38.5/3465₽│
  └──────────────┴──────────────┴──────────────┴────────────┘
  
  - График потребления токенов по дням
  - Средняя стоимость API на один заказ
  - Разбивка по моделям (pie chart)
  - Разбивка по назначению: генерация 60%, анализ 15%, чат 10%, рерайт 15%

Секция "ROI":
  - Доход / Расход API = коэффициент (например 45x)
  - Чистая прибыль по дням (line chart)
  - Прибыль по типам работ
  - Самые прибыльные заказы (топ-10)

Секция "Антиплагиат":
  - Средняя уникальность с первой попытки
  - % заказов, потребовавших рерайт
  - Среднее кол-во итераций рерайта
  - Распределение уникальности (histogram)
```

#### 5. УВЕДОМЛЕНИЯ (/dashboard/notifications)
```
Полный лог всех уведомлений с фильтрацией:
  [Все] [Заказы] [Сообщения] [Ошибки] [Система]

Каждое уведомление:
  - Иконка типа
  - Время
  - Текст
  - Связанный заказ (клик → карточка заказа)
  - Статус: прочитано / не прочитано

Пометить все как прочитанные.
Автоудаление уведомлений старше 30 дней.

WebSocket: новые уведомления появляются мгновенно без перезагрузки.
Звуковое оповещение при новом уведомлении (настраиваемое).
Browser push-notifications (если разрешены).
```

#### 6. ЛОГИ (/dashboard/logs)
```
Реалтайм лог всех действий бота (как терминал):

  [2026-02-08 21:45:12] [SCAN] Найдено 8 новых заказов
  [2026-02-08 21:45:15] [SCORE] Заказ #12345 — score 85, can_do=true
  [2026-02-08 21:45:18] [BID] Заказ #12345 — ставка 2800₽
  [2026-02-08 21:46:01] [SCAN] Найдено 3 новых заказа
  [2026-02-08 21:46:05] [SCORE] Заказ #12346 — score 35, can_do=false (чертёж)
  [2026-02-08 21:48:30] [ACCEPT] Заказ #12340 принят заказчиком
  [2026-02-08 21:48:32] [GENERATE] Начата генерация: Курсовая, ~30 стр
  [2026-02-08 21:52:10] [GENERATE] Завершено, 32 стр, 15 API вызовов, $0.42
  [2026-02-08 21:52:15] [PLAGIARISM] Проверка уникальности через ETXT...
  [2026-02-08 21:53:01] [PLAGIARISM] Уникальность: 72% (требуется 50%) ✅
  [2026-02-08 21:53:05] [DELIVER] Файл загружен в чат заказчика
  [2026-02-08 21:53:06] [CHAT] Отправлено: "Работа готова, загружаю..."

Фильтры: [Все] [SCAN] [SCORE] [BID] [GENERATE] [PLAGIARISM] [CHAT] [ERROR]
Поиск по тексту.
Экспорт в TXT.
Автоскролл (вкл/выкл).
```

#### 7. НАСТРОЙКИ (/dashboard/settings)
```
Секция "Бот":
  - 🟢 Автоматический режим (вкл/выкл) — главный тумблер
  - Интервал сканирования (30-300 сек, slider)
  - Макс заказов одновременно (1-10)
  - Рабочие часы (с __ до __, или 24/7)
  - Минимальный score для авто-ставки (0-100, slider)

Секция "Фильтры заказов":
  - Мин цена / Макс цена
  - Включённые типы работ (чекбоксы)
  - Включённые предметы (чекбоксы)
  - Мин/макс количество страниц
  - Макс ставок у заказа (чтобы не лезть в конкурентные)

Секция "Ценообразование":
  - Базовые цены по типам работ (редактируемая таблица)
  - Коэффициент от бюджета заказчика (85-100%, slider)
  - Коэффициент от средней ставки (85-110%, slider)

Секция "AI":
  - Основная модель (dropdown: gpt-4o, gpt-4o-mini, gpt-4-turbo)
  - Модель для анализа (dropdown)
  - Температура генерации (0.3-1.0)
  - Макс бюджет API в день ($)

Секция "Антиплагиат":
  - Система по умолчанию (ETXT / text.ru)
  - Макс итераций рерайта (1-5)
  - Добавочная уникальность (сколько % сверх требуемой для страховки)

Секция "Антибан":
  - Мин задержка между действиями (сек)
  - Макс задержка (сек)
  - Прокси (поле ввода)
  - User-Agent (авто / кастомный)

Секция "Шаблоны":
  - Шаблон комментария к ставке (textarea, поддержка переменных: {topic}, {work_type})
  - Шаблон приветствия в чате
  - Шаблон при отправке работы

Секция "Аккаунт":
  - Сменить пароль дашборда
  - API ключи (OpenAI, text.ru, ETXT) — маскированные поля
```

#### 8. ЧАТЫ (/dashboard/chats)
```
Список всех активных чатов с заказчиками.
Интерфейс как мессенджер:

Левая панель: список чатов
  - Имя заказчика / ID
  - Последнее сообщение (превью)
  - Бейдж непрочитанных
  - Статус заказа

Правая панель: переписка
  - Сообщения заказчика (слева)
  - Авто-ответы бота (справа, пометка "🤖 авто")
  - Ручные ответы (справа, без пометки)
  - Поле ввода для ручного ответа (override AI)
  - Кнопка "Отключить авто-ответ для этого чата"

Это позволяет вмешаться в переписку вручную, если AI
написал что-то не то или заказчик задаёт сложные вопросы.
```

---

## СЕКЦИЯ 12: База данных (src/database/)

### Модели (models.py)

```sql
-- Заказы
CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    avtor24_id VARCHAR(20) UNIQUE NOT NULL,
    title TEXT NOT NULL,
    work_type VARCHAR(100),
    subject VARCHAR(200),
    description TEXT,
    pages_min INT,
    pages_max INT,
    font_size INT DEFAULT 14,
    line_spacing FLOAT DEFAULT 1.5,
    required_uniqueness INT,
    antiplagiat_system VARCHAR(50),
    deadline TIMESTAMP,
    budget_rub INT,
    
    -- Наша работа
    bid_price INT,
    bid_comment TEXT,
    bid_placed_at TIMESTAMP,
    score INT,
    status VARCHAR(30) DEFAULT 'new',
    -- Статусы: new → scored → bid_placed → accepted → generating → 
    --          checking_plagiarism → rewriting → delivered → completed → rejected
    
    generated_file_path TEXT,
    uniqueness_percent FLOAT,
    
    -- Финансы
    income_rub INT,            -- сколько мы получили
    api_cost_usd FLOAT,        -- сколько потратили на API
    api_tokens_used INT,       -- сколько токенов потрачено
    
    -- Мета
    customer_username VARCHAR(100),
    error_message TEXT,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Сообщения чата
CREATE TABLE messages (
    id SERIAL PRIMARY KEY,
    order_id INT REFERENCES orders(id),
    direction VARCHAR(10),     -- 'incoming' или 'outgoing'
    text TEXT NOT NULL,
    is_auto_reply BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Логи действий
CREATE TABLE action_logs (
    id SERIAL PRIMARY KEY,
    action VARCHAR(50),        -- 'scan', 'bid', 'generate', 'deliver', 'chat', 'error'
    order_id INT REFERENCES orders(id),
    details TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Ежедневная статистика
CREATE TABLE daily_stats (
    id SERIAL PRIMARY KEY,
    date DATE UNIQUE NOT NULL,
    bids_placed INT DEFAULT 0,
    orders_accepted INT DEFAULT 0,
    orders_delivered INT DEFAULT 0,
    income_rub INT DEFAULT 0,
    api_cost_usd FLOAT DEFAULT 0,
    api_tokens_used INT DEFAULT 0
);

-- Уведомления (реалтайм на дашборде)
CREATE TABLE notifications (
    id SERIAL PRIMARY KEY,
    type VARCHAR(30) NOT NULL,     -- 'new_order', 'order_accepted', 'order_delivered',
                                   -- 'new_message', 'error', 'daily_summary'
    order_id INT REFERENCES orders(id),
    title TEXT NOT NULL,
    body JSONB NOT NULL,            -- полные данные уведомления
    is_read BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Настройки бота (персистентные, редактируемые через дашборд)
CREATE TABLE bot_settings (
    key VARCHAR(100) PRIMARY KEY,
    value TEXT NOT NULL,
    updated_at TIMESTAMP DEFAULT NOW()
);
-- Дефолтные значения вставляются при миграции

-- API usage трекинг (детальный)
CREATE TABLE api_usage (
    id SERIAL PRIMARY KEY,
    order_id INT REFERENCES orders(id),
    model VARCHAR(50) NOT NULL,           -- 'gpt-4o', 'gpt-4o-mini'
    purpose VARCHAR(30) NOT NULL,         -- 'generation', 'analysis', 'chat', 'rewrite', 'scoring'
    input_tokens INT NOT NULL,
    output_tokens INT NOT NULL,
    cost_usd FLOAT NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);
```

---

## СЕКЦИЯ 13: Главный цикл (src/main.py)

```python
# Архитектура: FastAPI + APScheduler + WebSocket

# Фоновые задачи:

# 1. Сканер заказов — каждые 60 сек
async def scan_orders_job():
    orders = await scraper.fetch_new_orders()
    for order in orders:
        if already_processed(order.id):
            continue
        score = await analyzer.score_order(order)
        if score.can_do and score.score >= MIN_SCORE:
            price = await analyzer.calculate_price(order)
            await scraper.place_bid(order, price)
            await notifications.push("new_order", order, price, score)  # → дашборд
            save_to_db(order, score, price)

# 2. Обработчик принятых заказов — каждые 120 сек
async def process_accepted_orders_job():
    accepted = await scraper.check_accepted_orders()
    for order in accepted:
        await notifications.push("order_accepted", order)  # → дашборд
        result = await generator.generate(order)
        uniqueness = await antiplagiat.check(result.file_path)
        
        attempts = 0
        while uniqueness < order.required_uniqueness and attempts < 3:
            result = await antiplagiat.rewrite_and_recheck(result)
            uniqueness = result.uniqueness
            attempts += 1
        
        await scraper.upload_and_send(order, result.file_path)
        await notifications.push("order_delivered", order, uniqueness)  # → дашборд

# 3. Чат-респондер — каждые 120 сек
async def chat_responder_job():
    new_messages = await scraper.check_new_messages()
    for msg in new_messages:
        response = await chat_ai.generate_response(msg)
        await scraper.send_message(msg.order_id, response)
        await notifications.push("new_message", msg, response)  # → дашборд

# 4. Ежедневная сводка — в 22:00
async def daily_summary_job():
    stats = await db.get_today_stats()
    await notifications.push("daily_summary", stats)  # → дашборд

# FastAPI эндпоинты:
# GET  /health                          — healthcheck для Railway
# GET  /dashboard/*                     — дашборд (HTML страницы)
# GET  /api/dashboard/stats             — статистика для виджетов
# GET  /api/dashboard/orders            — список заказов
# GET  /api/dashboard/orders/{id}       — детали заказа
# GET  /api/dashboard/analytics         — аналитика
# GET  /api/dashboard/notifications     — уведомления
# POST /api/dashboard/notifications/read — пометить прочитанными
# GET  /api/dashboard/logs              — логи
# GET  /api/dashboard/settings          — настройки
# PUT  /api/dashboard/settings          — обновить настройки
# POST /api/dashboard/orders/{id}/stop  — остановить обработку заказа
# POST /api/dashboard/orders/{id}/regen — перегенерировать работу
# POST /api/dashboard/chat/{id}/send    — ручное сообщение в чат
# WS   /ws/notifications                — WebSocket для реалтайм уведомлений
# WS   /ws/logs                         — WebSocket для реалтайм логов
# POST /api/dashboard/login             — авторизация
```

---

## СЕКЦИЯ 14: Деплой на Railway (авто-миграции + тесты)

### railway.toml

```toml
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
```

### Dockerfile

```dockerfile
FROM python:3.12-slim

# Node.js для docx-js
RUN curl -fsSL https://deb.nodesource.com/setup_20.x | bash - && \
    apt-get install -y nodejs chromium fonts-liberation libatk-bridge2.0-0 \
    libatk1.0-0 libcups2 libdrm2 libgbm1 libgtk-3-0 libnss3 libxcomposite1 \
    libxdamage1 libxrandr2 xdg-utils

# Playwright
ENV PLAYWRIGHT_BROWSERS_PATH=/browsers
RUN pip install playwright && playwright install chromium

# Зависимости Python
COPY requirements.txt .
RUN pip install -r requirements.txt

# Зависимости Node.js
COPY package.json .
RUN npm install

COPY . .

# Автоматические тесты + миграции при старте
# В production можно убрать pytest, оставив только миграции
CMD ["sh", "-c", "python -m alembic upgrade head && python -m uvicorn src.main:app --host 0.0.0.0 --port ${PORT:-8000}"]
```

### Для development (с тестами перед стартом):
```dockerfile
CMD ["sh", "-c", "pytest tests/ -x --tb=short && python -m alembic upgrade head && python -m uvicorn src.main:app --host 0.0.0.0 --port ${PORT:-8000}"]
```

### Railway сервисы:

```
1. avtor24-bot (основной сервис) — Web service
2. PostgreSQL — Railway built-in plugin (подключается через DATABASE_URL)
```

### Авто-миграции (Alembic)

```
КРИТИЧЕСКИ ВАЖНО: Все миграции должны выполняться автоматически.

Структура:
  alembic/
  ├── alembic.ini
  ├── env.py              # Читает DATABASE_URL из ENV
  └── versions/           # Файлы миграций

Процесс разработки:
1. Claude Code создаёт/меняет модели в src/database/models.py
2. Сразу генерирует миграцию:
   alembic revision --autogenerate -m "описание изменения"
3. Применяет локально для проверки:
   alembic upgrade head
4. При деплое на Railway — CMD в Dockerfile сначала делает
   `alembic upgrade head`, потом запускает приложение

Откат:
   alembic downgrade -1

Alembic env.py должен:
- Читать DATABASE_URL из переменной окружения
- Поддерживать async (asyncpg)
- Импортировать все модели из src/database/models.py
```

### Автоматические тесты

```
Запуск: pytest tests/ -v

Структура тестов:
  tests/
  ├── conftest.py              # Фикстуры: тестовая БД, мок OpenAI, мок Playwright
  ├── test_scraper.py          # Тесты парсера
  │   ├── test_parse_order_list()    — парсинг HTML ленты заказов
  │   ├── test_parse_order_detail()  — парсинг деталей заказа
  │   ├── test_bid_placement()       — постановка ставки (мок)
  │   └── test_session_management()  — сохранение/восстановление сессии
  │
  ├── test_analyzer.py         # Тесты анализатора
  │   ├── test_scoring()             — скоринг заказа возвращает валидный JSON
  │   ├── test_price_calculation()   — расчёт цены по разным сценариям
  │   └── test_file_analysis()       — извлечение текста из PDF/DOCX
  │
  ├── test_generator.py        # Тесты генератора
  │   ├── test_essay_generation()    — генерация эссе
  │   ├── test_coursework_plan()     — генерация оглавления курсовой
  │   ├── test_docx_creation()       — DOCX создаётся и валиден
  │   └── test_router_mapping()      — все типы работ замаплены на генераторы
  │
  ├── test_antiplagiat.py      # Тесты антиплагиата
  │   ├── test_check_uniqueness()    — API text.ru возвращает результат
  │   └── test_rewriter()            — рерайт повышает уникальность
  │
  ├── test_sandbox.py          # Тесты песочницы
  │   ├── test_python_execution()    — Python код запускается
  │   ├── test_timeout()             — таймаут работает
  │   ├── test_memory_limit()        — лимит памяти работает
  │   └── test_error_feedback()      — stderr передаётся корректно
  │
  ├── test_chat_ai.py          # Тесты чат-респондера
  │   ├── test_response_generation() — ответ генерируется
  │   ├── test_no_ai_mention()       — ответ не содержит упоминаний AI
  │   └── test_response_length()     — ответ не длиннее 3 предложений
  │
  ├── test_dashboard.py        # Тесты дашборда
  │   ├── test_login()               — логин работает
  │   ├── test_unauthorized()        — без токена → 401
  │   ├── test_stats_endpoint()      — /api/dashboard/stats возвращает данные
  │   └── test_websocket()           — WebSocket подключение работает
  │
  └── test_database.py         # Тесты БД
      ├── test_migrations()          — миграции применяются без ошибок
      ├── test_crud_orders()         — CRUD для заказов
      └── test_crud_notifications()  — CRUD для уведомлений

Мок-стратегия:
- OpenAI API → мок с фиксированными ответами (не тратить токены на тесты)
- Playwright → мок с сохранёнными HTML-страницами Автор24
- Антиплагиат API → мок с фиксированным % уникальности
- PostgreSQL → тестовая БД (SQLite in-memory для быстрых тестов)

CI на Railway:
- При каждом деплое: pytest запускается перед стартом приложения
- Если тесты падают → деплой отменяется
- CMD: "pytest tests/ -x --tb=short && alembic upgrade head && uvicorn..."
  (флаг -x = остановиться на первом падении)
```

---

## СЕКЦИЯ 15: Безопасность и антибан

```
1. Случайные задержки между всеми действиями (30-120 сек)
2. Не более 15-20 ставок в день (естественный лимит)
3. Ротация User-Agent
4. Российский прокси (резидентный, не датацентр)
5. Имитация человеческого поведения:
   - Случайные движения мыши перед кликом
   - Не работать ночью (22:00-07:00) — опционально
   - Не ставить ставки моментально после появления заказа (задержка 2-5 мин)
6. Мониторинг бана:
   - Если при парсинге получаем 403/captcha → пауза 30 мин
   - Уведомить в Telegram
   - Попробовать переавторизоваться
```

---

## СЕКЦИЯ 16: Порядок разработки (этапы)

```
ЭТАП 1 (День 1-2): Скелет + Инфраструктура
- [ ] Инициализация проекта, requirements.txt, Dockerfile, railway.toml
- [ ] Конфиг (.env + pydantic-settings)
- [ ] Database модели (SQLAlchemy)
- [ ] Alembic init + первая миграция (авто-генерация из моделей)
- [ ] FastAPI skeleton + healthcheck + WebSocket endpoint
- [ ] Тесты: test_database.py (миграции, CRUD)
- [ ] Деплой на Railway (пустое приложение + PostgreSQL плагин)
- [ ] Проверить: миграции выполняются при старте, /health отвечает

ЭТАП 2 (День 2-4): Скрапер
- [ ] Playwright browser manager с прокси
- [ ] Авторизация на Автор24 (cookies persistence)
- [ ] Парсинг ленты заказов
- [ ] Парсинг деталей заказа
- [ ] Скачивание прикреплённых файлов
- [ ] Постановка ставок
- [ ] Тесты: test_scraper.py (моки HTML-страниц)
- [ ] Миграция если нужны новые поля

ЭТАП 3 (День 4-6): Анализатор + Генератор (MVP)
- [ ] Скоринг заказов (GPT-4o-mini)
- [ ] Расчёт цен
- [ ] Генератор эссе (простейший тип)
- [ ] Генератор реферата
- [ ] DOCX builder (Node.js docx-js)
- [ ] API usage трекинг (запись токенов и стоимости в БД)
- [ ] Тесты: test_analyzer.py, test_generator.py
- [ ] Интеграция: заказ → анализ → генерация → DOCX

ЭТАП 4 (День 6-8): Антиплагиат + Чат
- [ ] Интеграция text.ru / ETXT API
- [ ] Рерайтер (цикл до уникальности)
- [ ] Чат-респондер (GPT-4o-mini)
- [ ] Загрузка готовой работы в чат
- [ ] Тесты: test_antiplagiat.py, test_chat_ai.py

ЭТАП 5 (День 8-10): Расширение генераторов
- [ ] Курсовые работы (пошаговая генерация по главам)
- [ ] Дипломные/ВКР
- [ ] Контрольные / Решение задач
- [ ] Презентации
- [ ] Переводы
- [ ] Все остальные типы (бизнес-планы, отчёты и т.д.)
- [ ] Тесты для каждого генератора

ЭТАП 6 (День 10-12): Песочница для кода
- [ ] Docker executor (запуск кода в контейнере)
- [ ] AI-цикл исправления (до 5 попыток)
- [ ] Поддержка Python, JS, Java, C++
- [ ] Тесты: test_sandbox.py

ЭТАП 7 (День 12-15): Дашборд (ОСНОВНОЙ ИНТЕРФЕЙС)
- [ ] Авторизация (JWT + httpOnly cookies)
- [ ] Overview — виджеты + графики + лента событий
- [ ] Заказы — таблица + фильтры + детали + экспорт
- [ ] Аналитика — доход, заказы, AI/токены, ROI, антиплагиат
- [ ] Уведомления — реалтайм через WebSocket + колокольчик
- [ ] Логи — реалтайм лог действий бота
- [ ] Настройки — все параметры бота через UI
- [ ] Чаты — мессенджер с возможностью ручного ответа
- [ ] Тесты: test_dashboard.py
- [ ] Browser push-notifications (опционально)

ЭТАП 8 (День 15-16): Полировка + Production
- [ ] Обработка ошибок (retry, graceful degradation)
- [ ] Антибан (задержки, ротация UA, мониторинг 403/captcha)
- [ ] Все тесты зелёные: pytest tests/ -v
- [ ] CI: тесты → миграции → запуск (в CMD Dockerfile)
- [ ] Финальный деплой на Railway
- [ ] Нагрузочное тестирование (5-10 заказов параллельно)
```

---

## СЕКЦИЯ 17: Ключевые зависимости

### requirements.txt
```
fastapi==0.115.0
uvicorn==0.32.0
playwright==1.49.0
openai==1.55.0
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
python-jose==3.3.0
passlib[bcrypt]==1.7.4
httpx==0.28.0
apscheduler==3.10.4
python-multipart==0.0.12
pymupdf==1.24.0
python-docx==1.1.2
aiofiles==24.1.0
websockets==13.1
pydantic-settings==2.6.0
Jinja2==3.1.4
pytest==8.3.4
pytest-asyncio==0.24.0
```

### package.json
```json
{
  "dependencies": {
    "docx": "^8.5.0"
  }
}
```
